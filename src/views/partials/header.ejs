ServeConnect
• Home
• Organizations
• Projects
• Categories
• <% if (isLoggedIn) { %>
    • My Dashboard
    • Logout
    • <% } else { %>
        • Register
        • Login
        • <% } %>
            <% if (typeof success !=='undefined' && success && success.length> 0) { %>
                <%= success[0] %>
                    <% } %>
                        <% if (typeof error !=='undefined' && error && error.length> 0) { %>
                            <%= error[0] %>
                                <% } %>